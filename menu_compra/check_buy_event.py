for event in pygame.event.get():
    if event.type == MOUSEBUTTONDOWN :
        mouse_pos = event.pos
        for button in buy_buttons
            if button.rect.collidepoint(mouse_pos):
                #aguardar novo click que diga onde criar o respectivo defensor

